<%= form_with(model: revoew, local: true) do |form| %>
  <% if revoew.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(revoew.errors.count, "error") %> prohibited this revoew from being saved:</h2>

      <ul>
      <% revoew.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rating %>
    <%= form.number_field :rating %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
